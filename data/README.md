This directory holds the downloaded price data.

Run `python ../data_download.py` from the project root to populate it with
`stock_prices.csv` (adjusted daily closes from Yahoo Finance via
`yfinance`).
